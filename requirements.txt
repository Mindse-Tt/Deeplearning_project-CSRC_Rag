# CSRC-RAG 运行依赖（团队整理）
# 说明：pyproject.toml 只声明了构建知识库/基线所需的轻量依赖（numpy / scikit-learn / openpyxl），
# 完整跑通「稠密检索 + 精排 + LoRA 生成 + Demo」还需要下面这套深度学习与检索栈。
# 目标环境：Python 3.11/3.12 + RTX 2060 SUPER 8GB（CUDA）。
# 注意：bitsandbytes 仅在 NVIDIA CUDA 环境可用；macOS / 纯 CPU 环境请跳过它，
#       并在 configs/qlora_config.json 中关闭 4-bit 量化后再训练。

# --- 深度学习 / 微调 ---
torch>=2.1
transformers==4.44.2
peft==0.13.2
accelerate==0.33.0
bitsandbytes==0.43.3 ; platform_system != "Darwin"   # CUDA-only（Windows/Linux GPU）；macOS 自动跳过
datasets>=2.19

# --- 检索 / 向量化 / 精排 ---
sentence-transformers>=2.5      # bge-small-zh-v1.5 稠密检索 + bge-reranker-v2-m3 精排
jieba>=0.42                     # 中文分词（BM25 词面召回，缺失会降级为 regex_bigram）

# --- Demo 服务 / 数据 ---
fastapi>=0.110
pydantic>=2.5
numpy>=1.24,<3
scikit-learn>=1.3,<2
openpyxl>=3.1,<4

# --- 可视化（论文图表生成：scripts/build_paper_figures.py / build_extra_figures.py）---
matplotlib>=3.7
