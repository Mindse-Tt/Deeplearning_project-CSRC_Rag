%%{init: {"theme":"dark"}}%%
xychart-beta
    title "LoRA QLoRA 训练 Loss 曲线 (PLACEHOLDER，M4 训完后用 loss.png 替换本图)"
    x-axis ["step 0", "step 200", "step 400", "step 600", "step 800", "step 1000", "step 1200"]
    y-axis "Cross Entropy Loss" 0 --> 4
    line [3.8, 2.4, 1.8, 1.5, 1.3, 1.25, 1.22]
    line [3.9, 2.6, 2.1, 1.9, 1.8, 1.78, 1.77]
```

> **说明**：LoRA train/val loss 曲线占位图。两条折线：①train loss（下方，预期光滑下降收敛到 ~1.2）②val loss（上方，预期轻微 gap 但同向下降，若 val 反弹超过 0.1 patience=2 早停）。训练超参：3 epoch × ~500 step ≈ 1500 step，cosine schedule + warmup 0.03，per_dev_batch=2、grad_accum=8、max_seq=2048。⚠️ 本图数据为**占位**；真实曲线由 `transformers.Trainer` 的 `log_history` 导出到 `artifacts/models/qwen_lora_csrc/loss.png`，M4 训完后在论文里直接替换为 PNG（也可用 matplotlib 画，mermaid 仅用于草图）。
